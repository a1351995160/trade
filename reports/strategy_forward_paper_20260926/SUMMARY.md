# 策略档案评审与前瞻观察开发验收

PR #21 已合入 main，合并点为 `4039d85fb527b93e27f041ce6d315914bbe99ec8`。本报告是在该基线上新增的策略评审、前瞻 Paper 与共享组合开发记录。

## 已有真实研究结果

源任务：`E:/llmwiki/autonomous-strategy-research-v1/bounded-research-20260926`。5 个候选均导出至 `E:/llmwiki/autonomous-strategy-research-v1/strategy-archives-20260926`。本目录 `REAL_STRATEGY_REVIEWS.json` 保存逐项评审、规则档案哈希和准入结论；原始研究证据索引见 `../bounded_research_20260926/EVIDENCE.json`。

| 候选 | 已曝光历史窗口净收益 | 工程观察准入 | 正式观察准入 |
|---|---:|---|---|
| CANDIDATE_001 | 8.62% | 允许 | 拒绝 |
| CANDIDATE_002 | 14.28% | 允许 | 拒绝 |
| CANDIDATE_003 | -2.33% | 允许 | 拒绝 |
| CANDIDATE_004 | 5.31% | 允许 | 拒绝 |
| CANDIDATE_005 | 15.45% | 允许 | 拒绝 |

以上只是已见样本上的账户表现，没有新做独立确认，也不能选择收益最高者宣称有效。工程观察准入取决于可执行性和账户证据完整性，不以盈利代替资格。

## 真实 Paper 状态

**真实观察天数：0；合格策略数：0。** 2026-09-26 检查本机已安装通达信量化模拟终端，但 `TdxW.exe` 未运行。遵循本地 TQ 技能，未继续发送行情 HTTP 请求，也未伪造当日快照或未来交易日成交。

本版提供真实采集入口与观察账户功能，当前未建立可宣称持续运行的真实 Paper。实际启动还需要：运行并登录通达信、取得观察日前完整预热数据与公司行动覆盖、核对交易所日历、在冻结窗口内采集并推进。任何缺失必须保留为缺口。

观察期间公司行动会停机，尚无前瞻分红入账；正式策略统计确认与正式观察仍未开放。合成进程故障测试只能证明测试场景下的工程行为，不能替代以上现实证据。

操作说明与业务架构图见 `../../docs/STRATEGY_FORWARD_PAPER_V1.md`。

## 本地验证

三个互不重叠的最终验证集合合计 **721 passed、21 skipped**：

- `regression.xml`：592 passed、21 skipped，覆盖指标、条件、退出、入口、既有修复与数据边界。跳过项为未启用的真实采样及本机符号链接能力限制。
- `lifecycle.xml`：95 passed，覆盖新生命周期模块与既有有界研究回归；其中 Paper 的18项场景包括目标仓位、真实引擎共用资金、交易前撤销、权威读取失败、恢复超时、撤销提交中断及真实子进程退出恢复。
- `supplemental.xml`：34 passed，覆盖统一命令入口、S1公共策略/价格恢复、诊断、运行协议、账户治理与后端迁移。

另有5份真实候选档案核验、真实评审 CLI 调用和 `git diff --check` 通过。pytest 结束时个别运行仍报告既有 Windows 临时目录清理权限错误，测试进程退出码为0；未将它计作业务验收失败或隐去。通达信 HTTP 真连接及未来观察均未验证。
