# BaoStock取数恢复决定 V1（待批准，不可执行）

## 已核实事实

2026-09-12检查：1354/5182只双价格响应成功，另1只仅RAW成功；前6批质量报告通过。第7批（batch=6）耗时900.079秒，timed_out=true，退出码1。没有运行中的同入口Python进程。

中断对象为002853.SZ的adjustflag=1请求，日期2022-07-22至2024-07-31。请求开始记录存在，响应及访问回执不存在。不得把它视为未请求过，也不能从不存在的文件恢复数据。记录不足以区分当时网络等待、服务端延迟或客户端读取阶段；900秒是整批期限，不证明单个请求阻塞900秒。

本计划全部既有资源回执累计3550.1620778秒（59.17分钟）；原5400秒上限剩1849.8379222秒（30.83分钟）。成功批次200只分别耗时227、289、535、540、668秒；剩3828只双响应不齐，按这些速度估算仅取数还需约72至213分钟，不含异常及检查，不能保证在剩余30.83分钟完成。此为工期估计，不是额外额度。

## 推荐的一次具体批准

1. 本计划累计资源上限从90分钟调整为360分钟，包含已经消费的59.17分钟；不改历史记录。两个账户执行累计仍最多1800秒。截止仍2026-09-14T10:05:03+08:00，较早约束优先。不新增任何价格表现曝光或策略额度。
2. 仅允许002853.SZ上述中断的HFQ请求再获取一次。另存attempt-2请求、响应及访问记录，绑定原started文件和失败worker回执真实哈希；不覆盖原件、不伪造旧请求完成。该次再次失败则停止，不循环重试。
3. 其余尚未请求的固定成员按原字段和窗口继续；已完成响应须哈希及查询身份一致才能复用。以后出现的新中断仍停止，不能自动套用本次单对象重试许可。
4. 获取调度改为每worker最多50只证券，仍并发1、900秒/2048MiB、数值线程1；原200只质量分组保留，每组齐备后核验。该调整降低单批触及上限的机会，不保证网络不再超时。
5. 实施版本化恢复记录与累计资源核对，合成验证成功缓存不重复请求、仅指定对象可重试、原失败保留、累计/到期/撤销限制后再执行。原批准包和READ_PLAN不覆盖，通过明确修订关联，不能手改旧账本。

## 继续条件

批准后接续同一5182成员、原RAW/HFQ分离、原质量及无收益可行性门槛。条件全部满足再沿原已批准主回测执行，不新增策略，不缩小股票池。当前未恢复网络访问、未注册新账户额度、未启动回测，正式完成及就绪标志保持false。

原合同明确“每证券每价格模式最多一次初次获取”和累计90分钟，因此单对象二次获取及总时间增加需要本次具体决定；不能解释为普通代码修复自动获得。单worker上限、统计阈值、账户规则均不放宽。
