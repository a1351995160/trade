# 累计研究代码交付

本次提交包含此前未提交的A股策略规则、固定窗口准备与恢复、输入验证、账户退出、ETF策略与统一策略接口、受限执行脚本、进度查询及合成测试。

统一执行入口与能力范围见 [现有账户统一接入完成说明](STRATEGY_INTERFACE_MIGRATION_V2.md)。历史专用脚本保留以便追溯，不意味着已有批准可用于重新执行当前源码。

## 验证

累计新增/修改测试首次执行：447通过、9失败，耗时181.26秒。9项均为历史合成授权测试因系统日期越过冻结截止而失败。仅对三个被复用的预算测试函数固定合成时钟，生产时钟、授权期限和拒绝条件不变；缓存失败项回归11项通过（2.89秒，包含此前缓存用例），没有扩大skip或timeout。

暂存代码通过git diff --check；发布前检查不包含行情、预算、实验结果或凭证。验证只使用测试数据，没有运行新的真实回测。

## 保留在本地

真实数据、研究结果、artifacts和包含绩效的progress历史不随代码推送。两个嵌入真实绩效数值的旧报告脚本 `review_weekly_sample_limited_v1.py`、`report_train_batches_21_24_v1.py` 也保留本机，不改写其历史批准或结果文本。

本提交不合并main，不授予任何新的研究或交易权限。需要恢复时对本次提交使用git revert；不要reset工作区或清除本地研究证据。
