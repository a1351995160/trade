# V4 studentized共同时间块检验：待批准原型

本轮仅沿用户V2的C类准备授权继续合成方法研究；承接78b3aeef221bd71afa1cf62744a76f993f3ceda2。V3失败证据及政策/lock保持，不是策略变体、真实试验或免费重算。此前13项V3/V4实现测试通过不能证明统计适用性。

## 相对V3的明确变化

保持主对象为每日实际成本后组合净收益，零假设E[r]≤0。V3比较均值；V4比较均值除以标准误的t统计量，且每次重抽样重新估计标准误。原始与重抽样均用Bartlett HAC：gamma[k]=sum(z[t]z[t-k])/n，SE²=(gamma[0]+2 sum((1-k/21)gamma[k],k=1..20))/n。使用固定lag20、不作小样本乘数修正。实现以两端零填充的21日移动和恒等式减少计算量，测试逐项对照直接自协方差公式。

共同stationary bootstrap平均块长20，每次10000抽样；重抽样输入先减去原均值。p使用(1+超过次数)/(10001)，退化或缺失标准误拒绝推断，不静默删除不利抽样。计算临时数组每500次分块，完整共同时间索引预先固定；此分块不改变重抽样方法。

V3的训练内四折、实际结束日purge、20session隔离和placebo、完整家族BY仍为待批准的合同上下文。本轮不实现真实runner，不改变原冻结验收阈值。均值标准化不是公司行动、数据时点或验证集污染的补救措施。

## 合成校准冻结设计

输出E:/llmwiki/autonomous-strategy-research-v1/blocker-resolution-v2/statistical-calibration-v4。预注册先绑定当前脚本、V3/V4模块及Wilson辅助函数来源的真实文件SHA，后生成数据。与V3相同的5场景、200次模拟、252session、3成员、随机种子、alpha0.05及单侧95% Wilson上界≤0.10诊断要求；不通过更换种子或阈值凑通过。每次模拟p值保存在明确标为合成的逐次账本，失败也保留。累计校准停止上限1800秒，非真实研究预算或到期延期。

即使有限合成校准落在诊断带内，也不证明实际收益弱平稳、固定20lag足够或历史家族完整。块长/lag固定的有限样本实验不等于一般渐近一致性定理；真实样本长度与依赖结构不在当前许可下已验证。旧Validation仍是已曝光数据，不能重新命名为独立样本外。

## 原理依据

- [arch官方置信区间说明](https://arch.readthedocs.io/en/latest/bootstrap/confidence-intervals.html)：studentized bootstrap需要每个重抽样的标准误估计，可由解析标准误或嵌套抽样提供。本实现选用明确定义的HAC估计，不新增嵌套抽样。
- [statsmodels官方HAC说明](https://www.statsmodels.org/stable/generated/statsmodels.stats.sandwich_covariance.cov_hac.html)：提供Bartlett权重及lag定义的核对依据。本项目未新增该依赖，未声称调用其默认小样本修正。

以上支持方法构件，不能代替本项目校准或计划确认。详细结果以固定输出目录SYNTHETIC_CALIBRATION.json为准；若文件缺失，表示本轮未完整完成，不推断通过。

## 实际校准结论

本次完整运行281.107秒，1000条合成逐次记录与汇总一致，4个预注册来源SHA核验一致。IID/AR0.6/重叠10日/t5的raw拒绝次数分别18/13/7/7（各200次），家族任一拒绝次数13/9/4/6。IID raw单侧上界0.128985，未通过事前诊断；其余零均值场景在诊断带内。已知正均值200/200仅是功效结果。整体CALIBRATION_LIMITATION_NOT_APPROVED，不可用部分改善抵消失败场景，不接真实runner。

V3与V4实现合计13项测试通过；独立公式对照支持代码计算一致性，不证明总体推断有效。真实数据可得性、历史授权、额度和独立确认样本仍未解决。下一步须定点取得原确认包所列权威/来源证据；不能在缺少这些前提时把继续试方法或合成通过当作研究资格。
